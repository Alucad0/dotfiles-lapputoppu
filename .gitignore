# App-written leftovers that land next to the tracked configs
*.bak
*.bak[0-9]
*.orig
*~
*.swp

# Python bytecode (scripts under waybar/ are run directly, but a py_compile
# or import leaves these, and install.sh would link them)
__pycache__/
