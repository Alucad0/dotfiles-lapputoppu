#!/usr/bin/env bash
# Interactive screenshot: drag a region with slurp, save it with grim
set -euo pipefail
out="$HOME/Pictures/screenshots"
mkdir -p "$out"
grim -g "$(slurp)" "$out/screenshot_$(date +%Y%m%d_%H%M%S).png"
